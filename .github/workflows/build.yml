name: Automated Cydia Repo Builder for iOS 8

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Prepare Package Structure
      run: |
        rm -rf build-folder
        mkdir -p build-folder/Applications/iOS6Chat.app
        mkdir -p build-folder/DEBIAN
        
        # Генерируем чистый control (поднимаем версию до 1.6.0)
        cat << 'EOF' > build-folder/DEBIAN/control
        Package: com.okkidididjdj.ios6chat
        Name: iMessage iOS 6
        Version: 1.6.0
        Architecture: iphoneos-arm
        Description: Скевоморфный мессенджер. Фикс регистрации иконки.
        Maintainer: okkidididjdj
        Author: okkidididjdj
        Section: Applications
        
        EOF

        # Копируем веб-интерфейс
        cp Applications/iOS6Chat.app/index.html build-folder/Applications/iOS6Chat.app/
        
        # Дублируем иконки во всех возможных регистрах, чтобы iOS 8 их точно увидела
        cp CydiaIcon.png build-folder/Applications/iOS6Chat.app/icon.png || true
        cp CydiaIcon.png build-folder/Applications/iOS6Chat.app/icon@2x.png || true
        cp CydiaIcon.png build-folder/Applications/iOS6Chat.app/Icon.png || true
        cp CydiaIcon.png build-folder/Applications/iOS6Chat.app/Icon@2x.png || true
        cp CydiaIcon.png build-folder/Applications/iOS6Chat.app/AppIcon60x60@2x.png || true

        # СОЗДАЕМ СТРОГИЙ INFO.PLIST
        cat << 'EOF' > build-folder/Applications/iOS6Chat.app/Info.plist
        <?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
        <plist version="1.0">
        <dict>
            <key>CFBundleDevelopmentRegion</key>
            <string>English</string>
            <key>CFBundleExecutable</key>
            <string>iOS6Chat</string>
            <key>CFBundleIdentifier</key>
            <string>com.okkidididjdj.ios6chat</string>
            <key>CFBundleInfoDictionaryVersion</key>
            <string>6.0</string>
            <key>CFBundleName</key>
            <string>iOS6Chat</string>
            <key>CFBundlePackageType</key>
            <string>APPL</string>
            <key>CFBundleSignature</key>
            <string>????</string>
            <key>CFBundleSupportedPlatforms</key>
                <array>
                <string>iPhoneOS</string>
            </array>
            <key>CFBundleVersion</key>
            <string>1.6</string>
            <key>LSRequiresIPhoneOS</key>
            <true/>
            <key>UIDeviceFamily</key>
            <array>
                <integer>1</integer>
            </array>
        </dict>
        </plist>
        EOF

        # ЖЕЛЕЗНЫЙ ХАК ДЛЯ iOS 8:
        # В качестве исполняемого файла мы создаем команду, которая симитирует нативный запуск через bash
        echo -e '#!/bin/sh\n/usr/bin/uiopen "file:///Applications/iOS6Chat.app/index.html"' > build-folder/Applications/iOS6Chat.app/iOS6Chat

        # Скрипт после установки, который ПРИНУДИТЕЛЬНО перезапустит рабочий стол
        cat << 'EOF' > build-folder/DEBIAN/postinst
        #!/bin/sh
        chmod 755 /Applications/iOS6Chat.app/iOS6Chat
        
        # Регистрируем приложение в системе
        su -c uicache mobile
        
        # Даем системе 2 секунды и мягко перезагружаем рабочий стол (Respring)
        # Это заставит iOS 8 принудительно перерисовать экран и вывести иконку
        (sleep 2 && killall SpringBoard) &
        exit 0
        EOF

        # Выставляем права
        chmod 755 build-folder/DEBIAN
        chmod 644 build-folder/DEBIAN/control
        chmod 755 build-folder/DEBIAN/postinst
        chmod 755 build-folder/Applications/iOS6Chat.app
        chmod 755 build-folder/Applications/iOS6Chat.app/iOS6Chat
        chmod 644 build-folder/Applications/iOS6Chat.app/Info.plist
        chmod 644 build-folder/Applications/iOS6Chat.app/index.html

    - name: Build .deb Package
      run: |
        mkdir -p debs
        dpkg-deb -Zgzip --root-owner-group --build build-folder debs/ios6chat.brand.deb

    - name: Update Cydia Indexes
      run: |
        echo "Package: com.okkidididjdj.ios6chat" > Packages
        echo "Name: iMessage iOS 6" >> Packages
        echo "Version: 1.6.0" >> Packages
        echo "Architecture: iphoneos-arm" >> Packages
        echo "Description: Скевоморфный мессенджер. Жесткий сброс кэша SpringBoard." >> Packages
        echo "Maintainer: okkidididjdj" >> Packages
        echo "Author: okkidididjdj" >> Packages
        echo "Section: Applications" >> Packages
        echo "Size: $(stat -c%s debs/ios6chat.brand.deb)" >> Packages
        echo "Filename: debs/ios6chat.brand.deb" >> Packages
        echo "MD5sum: $(md5sum debs/ios6chat.brand.deb | awk '{print $1}')" >> Packages
        echo "" >> Packages
        
        bzip2 -c Packages > Packages.bz2

    - name: Commit and Push changes
      run: |
        git config --global user.name "GitHub Actions"
        git config --global user.email "actions@github.com"
        git add -f debs/ios6chat.brand.deb Packages Packages.bz2
        git commit -m "Исправление регистрации иконки для iOS 8 (v1.6.0)" || exit 0
        git push origin main --force



